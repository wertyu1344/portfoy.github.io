name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Varsayılan branch adınız "main" değilse, değiştirin (örn. "master")
  workflow_dispatch: # Manuel olarak da çalıştırılabilir

# GitHub Pages'e dağıtım için özel izinler
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🔐️
        uses: actions/checkout@v3

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies 💶
        run: npm ci

      - name: Build 🔧
        run: npm run build

      - name: Deploy 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          cname: # Özel alan adınız varsa buraya ekleyin
