name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Varsayılan branch adınız "main" değilse, değiştirin (örn. "master")
  workflow_dispatch: # Manuel olarak da çalıştırılabilir

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies 📦
        run: npm ci

      - name: Build 🔧
        run: npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: out # Next.js export çıktısının bulunduğu klasör
          branch: gh-pages # GitHub Pages için kullanılacak branch
